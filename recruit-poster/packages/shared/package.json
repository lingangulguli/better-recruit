{
  "name": "@recruit-poster/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared data models, scene configs, templates and mock AI service",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"No build step for shared (consumed as source)\""
  }
}
