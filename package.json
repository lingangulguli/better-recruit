{
  "name": "recruit-poster",
  "version": "0.1.0",
  "private": true,
  "description": "一键生成招募海报 - 面向早期创业团队与校园组织的免费工具",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:h5": "npm --workspace packages/h5 run dev",
    "build:h5": "npm --workspace packages/h5 run build",
    "preview:h5": "npm --workspace packages/h5 run preview",
    "build:shared": "npm --workspace packages/shared run build",
    "lint": "echo \"TODO: add eslint\" && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
